#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main() {
    unordered_set<int> st; // -< NOT SORTED, BUT UNIQUE. >- 
// lower_bound and upper_bound function
// does not works, rest all funtions are same.
// as above, it does not store in any particular
// order it has a better complexity, than set 
// in most cases, except some when collision happens.
} //! COMPLEXITY IS O(1).
// IN WORST CASE O(N).